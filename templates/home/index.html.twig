{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Controller  {{ controller_name }}! ✅</h1>

    <h5> Intrucciones </h5>
    
    <p> En esta aplicación, realizada en Symfony CLI version 5.4.19, existen dos entidades:Paciente y Tratamiento. La entidad Paciente guarda una relación muchos a muchos con la entidad Tratamientos.
    Se parte de la base de que sobre un nuevo paciente se aplican uno, ninguno o muchos 
    tratamientos.</p>
    <p>This are your app options:
</p>
    <ul>
       
           <li><code><a href="{{ path('app_paciente_index') }}"> Panel de pacientes</a></code></li>
        <li><code><a href="{{ path('app_tratamiento_index') }}"> Panel de tratamientos</a></code></li>
       
    </ul>
 <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre de paciente</th>
                 <th>Tratamientos aplicados</th>
               
            </tr>
        </thead>
        <tbody>
      {% for paciente in pacientes %}
        <tr>
          <td>{{ paciente.id }}</td>
          <td>{{ paciente.nombre }}</td>
          <td>
            {% for tratamiento in paciente.tratamientos %}
              {{ tratamiento.nombre }}
              {% if not loop.last %}, {% endif %}
            {% endfor %}
          </td>
        </tr>
      {% endfor %}
        </tbody>
    </table>



</div>
{% endblock %}
